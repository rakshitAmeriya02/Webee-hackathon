export enum FIELD_TYPE {
  SMALL_TEXT = "text",
  LONG_TEXT = "text_area",
  NUMBER = "number",
  DATE = "date",
}

export enum APP_ROUTES {
  ROOT = "/",
  TYPES = "/types",
  TYPE = "/type/:typeId",
}
